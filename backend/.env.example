# Local development (default fallback):
# DATABASE_URL=sqlite:///../database.db

# Cloud SQL PostgreSQL (via Cloud Run + Cloud SQL connection):
# Replace placeholders with your actual values.
DATABASE_URL=postgresql+psycopg2://DB_USER:DB_PASSWORD@/DB_NAME?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
